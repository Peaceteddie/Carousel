export const DEFAULT_RATE_LIMIT = 800;
export const DEFAULT_MAX_DEPTH = 1;
export const DEFAULT_MIN_IMAGE_SIZE = 1000;
export const DEFAULT_MAX_CONCURRENT_FETCHES = 3;
export const DEFAULT_EXCLUDE_PATTERNS = [
    '/ads/',
    '/banners/',
    '/promotions/',
    '/sponsored/',
];

export const config = {
    DEFAULT_RATE_LIMIT,
    DEFAULT_MAX_DEPTH,
    DEFAULT_MIN_IMAGE_SIZE,
    DEFAULT_MAX_CONCURRENT_FETCHES,
    DEFAULT_EXCLUDE_PATTERNS,
};
